*.code-workspace
__pycache__/
*.pyc

# compiled Go binary (built in Docker; never commit)
/authzen-adapter/authzen-pdp

# local Claude Code session data (but version skills)
.claude/*
!.claude/skills/

# build/dependency caches — also keeps 'railway up' payloads small
.spm-cache/
.terraform/
.DS_Store
